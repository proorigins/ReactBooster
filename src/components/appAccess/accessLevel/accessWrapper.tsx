import React, {FC} from "react";
import {AccessRoutes} from "./accessRoutes";

export const AccessWrapper: FC = ({children}) => {
    return (
        <>
          <AccessRoutes/>
        </>
    )
};
